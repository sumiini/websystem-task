extends layout

block content
    h1(id="htitle") Movies
    ul
        each movie in movies
            div(id="content")
                img(src=`${movie.url}`)
                br
                div
                h2 #{movie.title} / #{movie.year}
                br
                br
                br










