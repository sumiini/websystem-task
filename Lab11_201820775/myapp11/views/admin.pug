extends layout

block content
    h1(id="adminmovie") Admin Movies
    ul(id="adminmovielist")
        each movie in movies
            div(id="content")
                img(src=`${movie.url}`)
                br
                div
                h2(id="movie"+movie._id) #{movie.title} / #{movie.year}
                br

                form(method="GET" action="/routes/movie/read/"+movie._id)
                    input(type="submit" onclick="edit("+movie._id+",'"+movie.title+"','"+movie.year+"','"+movie.url+"')" value="EditData")

                //button
                //    a(href="/delete/"+movie._id) Delete
                form(method="POST" action="/routes/movie/delete/"+movie._id)
                    input(type="submit" onclick="ajaxDelete("+movie._id+")" value="Delete")
                //form(method="post" action="/routes/movie/delete/:id")
                //    input(type="submit" onclick="deletedata("+movie._id+")" value="DeleteData")



                form(method="POST" action="/routes/movie/update/:id")
                    input(type="submit" value="인기영화")
                br